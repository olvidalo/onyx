# Authentik Configuration
# Copy this to .env.authentik and fill in your values
# Then source it or include in your main .env

# =============================================================================
# Authentik Secrets (generate these)
# =============================================================================

# Generate with: openssl rand -base64 36
AUTHENTIK_SECRET_KEY=

# Database password (generate a strong password)
AUTHENTIK_DB_PASS=

# =============================================================================
# LDAP Connection (your existing LDAP server)
# =============================================================================

# LDAP server hostname
LDAP_HOST=ldap.uni-koeln.de

# Bind DN (service account for LDAP queries)
LDAP_USER=cn=serviceaccount,dc=uni-koeln,dc=de

# Bind password
LDAP_PASS=

# =============================================================================
# Optional: Port Configuration
# =============================================================================

# Authentik web UI port (default: 9000)
# AUTHENTIK_PORT=9000

# Authentik HTTPS port (default: 9443)
# AUTHENTIK_PORT_HTTPS=9443

# =============================================================================
# Onyx OIDC Configuration (fill in after Authentik setup)
# =============================================================================

# After configuring Authentik, add these to your Onyx .env:
#
# AUTH_TYPE=oidc
# OIDC_CLIENT_ID=<from Authentik>
# OIDC_CLIENT_SECRET=<from Authentik>
# OIDC_ISSUER=https://your-authentik-domain/application/o/onyx/
#
# Optional - map LDAP groups to Onyx roles:
# OIDC_GROUPS_ATTRIBUTE=groups
